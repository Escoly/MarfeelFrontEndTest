
const errorMessage = {
    template: () => {
        return (
            `<div class="errorMessageHidden" id="errorMessage">
                <p>Does not exist</p>
            </div>`
        )

    }
}

export default errorMessage;
